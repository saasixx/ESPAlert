{
  "name": "espalert-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "dev:docker": "docker compose up --build"
  },
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1",
    "turbo": "^2.8.12"
  },
  "packageManager": "npm@11.11.0"
}
